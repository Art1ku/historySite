# History Project

A small React + Vite web app showcasing an animated landing page about the medieval Turkic poet and philosopher **Jusup Balasaguni**. It uses modern UI tooling (React 19, Tailwind CSS, motion animations) and is built with Vite.

## 🚀 Quick Start

1. Install dependencies:

```bash
npm install
```

2. Start the dev server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```

4. Preview the production build:

```bash
npm run preview
```

## 🧰 Tech Stack

- **Framework:** React 19
- **Bundler:** Vite
- **Styling:** Tailwind CSS
- **Animations:** `motion` (framer-motion-inspired)
- **Icons:** `lucide-react`

## 📁 Project Structure

- `src/` – React application source
- `public/` – static assets
- `index.html` – Vite application entry template

## 🔎 Linting

Run TypeScript type-checking (no emit):

```bash
npm run lint
```

## ✅ Notes

- This project can be deployed to any static hosting provider that supports Vite builds (Netlify, Vercel, GitHub Pages, etc.).
- If you add server-side logic (Express, database, etc.), be sure to update this README with relevant setup steps.
